# Frontend — Interface de Assinatura

Interface simples com um botão para acionar a assinatura digital via chamada à API.

- `src/` — código-fonte da interface

Empacotado via `Dockerfile` para deploy no cluster Kubernetes, servido atrás do mesmo Ingress da API.
